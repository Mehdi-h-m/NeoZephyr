[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "neozephyr"
version = "0.1.0"
description = "AI coding agent"
requires-python = ">=3.11"

[project.scripts]
neo = "neozephyr.main:main"